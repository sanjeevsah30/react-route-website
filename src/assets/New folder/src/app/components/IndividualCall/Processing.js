import React from "react";
import { Result } from "antd";

export default function Processing() {
    return (
        <Result
            title="Info will be available shortly"
            subTitle="We are processing your transcript at the moment. This may take some time!"
        />
    );
}
